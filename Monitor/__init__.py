"""Graph of Trace — Monitor package.

Backend for recording scientific-agent execution traces into a Graph-of-Trace
(GoT) DAG. Agent-agnostic: any MCP-capable agent can call the `build_trace`
tool, and the output location is fully configurable (see config/config.yaml).
"""
